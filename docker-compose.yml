version: '3.8'

services:

  mysql:
    image: mysql:8.0

    container_name: laravel_mysql

    restart: always

    ports:
      - "3307:3306"

    environment:
      MYSQL_ROOT_PASSWORD: Amir11022013
      MYSQL_DATABASE: laravel_db

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: